import {makeStyles} from '@material-ui/core/styles'

export const useStyles = makeStyles({
    root:{
        '& > *': {
            margin: '.5rem',
            color:'#fff',
          },
    },
})